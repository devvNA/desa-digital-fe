## Current Understanding

- The upload button in `src/views/social-assistance/SocialAssistanceEditView.vue` throws `TypeError: $setup.fileInput?.click is not a function`.
- The current template binds `ref="fileInput"` to the shared `Input` component, then calls `fileInput?.click()` from the upload button.
- `src/components/ui/Input.vue` is a Vue component wrapper, not a native `<input>` element, so the ref resolves to a component instance/proxy that does not expose `.click()`.
- The shared `Input` component also does not define `name` or `accept` props, so using it as a file input is structurally mismatched.

## Target Change

- Restore the thumbnail uploader to use a native hidden `<input type="file">` in `src/views/social-assistance/SocialAssistanceEditView.vue`.
- Keep the visible upload button unchanged, but make its click handler safely trigger the native file input.
- Preserve existing preview behavior in `handleImageChange()` and current validation/error display.

## Root Cause

- The ref target is wrong: `fileInput` currently points to the `Input` Vue component instead of an HTML input element.
- Calling `.click()` on the component instance triggers the runtime error shown in QA.

## Risk Points

- The file input must stay visually hidden without breaking clickability.
- The upload flow should still pass the selected `File` into `socialAssistance.thumbnail` and generate preview URLs.
- Avoid changing unrelated form behavior or the shared `Input.vue` API, since this issue is isolated to the thumbnail uploader.

## Execution Steps

1. In `src/views/social-assistance/SocialAssistanceEditView.vue`, replace the current `Input ref="fileInput" type="file" ...` usage with a native `<input ref="fileInput" type="file" name="thumbnail" accept="image/*">`.
2. Keep the upload button and update its handler to call the native ref safely, e.g. `fileInput.value?.click()` through a small helper function or inline access that targets the DOM node.
3. Verify the surrounding classes still keep the real file input hidden and the button remains the visible trigger.
4. Confirm `handleImageChange()` still receives the change event and updates `socialAssistance.thumbnail` plus `thumbnail_url` preview.
5. Recheck template line references for thumbnail field error rendering to ensure no regressions in validation display.

## Expected Outcome

- Clicking `Upload` opens the system file picker.
- No Vue runtime warning or `click is not a function` error appears.
- Choosing an image still updates the preview and preserves existing submit behavior.
